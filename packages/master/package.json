{
  "name": "@zy-kit/master",
  "type": "module",
  "version": "0.0.0",
  "main": "master.css.ts",
  "scripts": {
    "build": "node --experimental-specifier-resolution=node --loader ts-node/esm scripts/build.ts && prettier ./dist --write",
    "runner": "node --experimental-specifier-resolution=node --loader ts-node/esm scripts/runner.ts",
    "lint": "eslint . --ext .ts",
    "extract": "mcss extract"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
